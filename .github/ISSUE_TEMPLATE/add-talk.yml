# Generated by scripts/generate-issue-templates.mjs
# Run `pixi run generate-issue-templates` to regenerate.

name: Add Talk
description: Add a new talk to the talks list.
labels:
  - talk
body:
  - type: markdown
    attributes:
      value: "<!-- @claude template: add-talk -->"
  - type: dropdown
    id: type
    attributes:
      label: Talk type
      description: Seminar / Colloquium / Plenary = invited; Parallel / Poster = contributed.
      options:
        - Seminar
        - Colloquium
        - Plenary
        - Parallel
        - Poster
    validations:
      required: true
  - type: input
    id: venue
    attributes:
      label: Venue
      placeholder: e.g. University of Hawaii
    validations:
      required: true
  - type: input
    id: date
    attributes:
      label: Date
      description: Machine-parseable format (e.g. 2025-06). Used for sorting and display.
      placeholder: YYYY-MM
    validations:
      required: true
  - type: input
    id: url
    attributes:
      label: URL
      description: Link to slides or event page. Optional.
      placeholder: https://...
    validations:
      required: false
